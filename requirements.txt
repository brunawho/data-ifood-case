# Dependências do pipeline.
#
# IMPORTANTE: no Databricks, NADA deste arquivo precisa ser instalado. O runtime
# já provê pyspark, delta e requests. Este arquivo existe para execução local e
# para deixar explícitas as versões contra as quais o código foi escrito.
#
# Instalação local:
#     pip install -r requirements.txt
#
# Requer JDK 8, 11 ou 17 — o PySpark 3.5 não funciona em Java 21+.

# --- Ingestão -------------------------------------------------------------- #
# Download dos arquivos da NYC TLC. Único uso: HTTP com stream e retry.
requests==2.32.3

# --- Processamento --------------------------------------------------------- #
# delta-spark 3.2 é a versão compatível com PySpark 3.5. O par de versões
# importa: combinações fora da matriz oficial falham na inicialização da sessão.
pyspark==3.5.1
delta-spark==3.2.0

# --- Testes ---------------------------------------------------------------- #
# Os testes de `tests/test_silver_regras.py` exigem PySpark; os demais rodam
# apenas com pytest.
pytest==8.3.3
